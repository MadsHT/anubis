[pytest]
# Pytest configuration for Anubis RAG Engine - MINIMAL for CI

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Minimal asyncio - don't auto-detect, just warn
asyncio_mode = legacy

# Minimal options
addopts = -v --tb=line
